apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mqtt-device-manager.serviceAccountName" . }}
  labels:
    {{- include "mqtt-device-manager.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
