{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "mqtt-device-manager.fullname" . }}-manager-rolebinding
  labels:
    {{- include "mqtt-device-manager.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "mqtt-device-manager.fullname" . }}-manager-role
subjects:
- kind: ServiceAccount
  name: {{ include "mqtt-device-manager.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
